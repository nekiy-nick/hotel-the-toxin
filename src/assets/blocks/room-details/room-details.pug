mixin room-details(options)
  if(!options)
    - options = {}
  if(!options.roomNumber)
    - options.roomNumber = "000"
  if(!options.roomPrice)
    - options.roomPrice = "refine"
  -
    let roomNumber = "room number"
    let roomStatus = "room status"
    let roomPrice = "room price"
  
  div(class="room-details")
      p(class="room-details__room-number-container")
        span(class="room-details__room-number")= options.roomNumber
        span(class="room-details__room-status")= options.roomStatus
      p(class="room-details__room-price-container")
        span(class="room-details__room-price")= options.roomPrice
        span(class="room-details__period") в сутки